----------------
----  MISC  ----
----------------

hl.config({
    misc = {
        force_default_wallpaper      = -1,
        disable_hyprland_logo        = true,
        animate_manual_resizes       = true,
        animate_mouse_windowdragging = true,
        enable_swallow               = true,
        swallow_regex                = "^(kitty)$",
        focus_on_activate            = false,
        -- vfr is now always on by default, no need to set it
    },
})
